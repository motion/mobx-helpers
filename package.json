{
  "name": "motion-mobx-helpers",
  "version": "1.0.2",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -r lib; babel src --out-dir lib",
    "watch": "npm run build -- --watch"
  },
  "author": "Motion <natewienert@gmail.com> (motion.io)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-steelbrain": "^4.0.2"
  },
  "dependencies": {
    "mobx": "^3.0.0"
  }
}
